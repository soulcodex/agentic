#!/usr/bin/env bash
# opencode.sh — Vendor generation for OpenCode
gen_opencode() {
  echo "  Generating Opencode adapter..."
  echo "  OpenCode reads AGENTS.md natively."
  echo "  No OpenCode project config is generated or managed by agentic."
}
