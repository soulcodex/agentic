#!/usr/bin/env bash
# codex/mcp.sh — Placeholder for Codex-specific MCP target logic.
# Codex currently uses .mcp.json directly (Claude-compatible shape), so no
# additional transformation beyond seed_claude_mcp_target is required.
